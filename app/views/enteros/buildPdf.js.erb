var $rows = $('.tbone').find('tr');
var enteros = [];

$rows.each(
  function(i,el){
    var $col = $(this).find('td');
    enteros.push($col.eq(0).text());
  });
console.log();
var url = '/enteros/showEntero?totals=' + enteros + '&tocken=' + $('input#token').val() +'&f=' + $('input#f').val();

$('#myIframe').attr("src", url);
$('.tag').show();
$('.pdfcol').show();
//window.location.href=url;
